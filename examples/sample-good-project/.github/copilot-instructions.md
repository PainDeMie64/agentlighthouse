# Copilot Instructions

Use `pnpm install`, `pnpm lint`, `pnpm typecheck`, and `pnpm test`.

Keep changes in `src/` unless docs or benchmarks need updates. Never expose secrets, credentials, tokens, or private data.
